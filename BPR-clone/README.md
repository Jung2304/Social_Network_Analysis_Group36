# BPR Project

Project files in this repository:

- source code and main project write-up: `RecBole/README.md`
- training logs: `log/`
- model checkpoints: `saved/`

Main document path:

- consolidated README / report: `RecBole/README.md`

Note: `log/` and `saved/` are runtime artifacts and should not be pushed to the project branch.
